export interface FeatureGRC {
	title: string;
	description: string;
	icon: React.ReactNode;
}
export enum sheetNameTypes {
	ccpaForm = 'contactUs',
	getInTouch = 'getInTouch',
	demoRequest = 'demoRequest'
      }